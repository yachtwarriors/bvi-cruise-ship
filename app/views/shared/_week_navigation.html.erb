<%# Locals: prev_url, next_url, today_url, form_url, start_date, end_date, today %>
<div class="flex items-center justify-between mb-4">
  <%= link_to prev_url, class: "inline-flex items-center px-3 py-2 text-sm font-medium text-slate-700 bg-white rounded-lg border border-slate-200 hover:bg-slate-50 transition" do %>
    <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/></svg>
    Prev
  <% end %>

  <div class="text-center">
    <h2 class="text-lg font-semibold text-slate-900">
      <%= start_date.strftime("%b %-d") %> – <%= end_date.strftime("%b %-d, %Y") %>
    </h2>
    <% if start_date == today %>
      <p class="text-xs text-cyan-600 font-medium">Next 7 Days</p>
    <% else %>
      <%= link_to "Back to Today", today_url, class: "text-xs text-cyan-600 hover:text-cyan-700 font-medium" %>
    <% end %>
  </div>

  <%= link_to next_url, class: "inline-flex items-center px-3 py-2 text-sm font-medium text-slate-700 bg-white rounded-lg border border-slate-200 hover:bg-slate-50 transition" do %>
    Next
    <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
  <% end %>
</div>

<%# Jump to date %>
<div class="mb-6 text-center">
  <%= form_with url: form_url, method: :get, class: "inline-flex items-center gap-2" do |f| %>
    <%= f.date_field :start_date, value: start_date, class: "text-sm border border-slate-200 rounded-lg px-3 py-1.5 text-slate-700 focus:ring-2 focus:ring-cyan-500 focus:border-cyan-500" %>
    <%= f.submit "Go", class: "px-3 py-1.5 text-sm font-medium text-white bg-cyan-500 rounded-lg hover:bg-cyan-600 cursor-pointer transition" %>
  <% end %>
</div>
